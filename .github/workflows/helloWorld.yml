name: Hello World
run-name: ${{github.actor}} says Hello World
on: workflow_dispatch
jobs:
  print-hello-world:
    runs-on: ubuntu-latest
    steps: 
      - name: Print Hello
        run: node --version
        run: Hello World